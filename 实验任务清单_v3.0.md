# 高光谱去雾实验任务清单（物理融合决策版）
# 版本: v3.0
# 创建日期: 2026-08-31
# 上游: 实验任务清单_v2.0.md（§1-§11 证据）、results/experiment_summary.md §10/§11
# 主题: 用物理规律提升扩散去雾——四种注入方式的决策与执行清单
# 统一评测口径: best ckpt + blend=1.0 + 固定采样种子 + 全测试集 264 对 + 多种子 mean±std

================================================================================
〇、当前坐标系（先看这里）
================================================================================

| 目标 | PSNR(dB) | 说明 |
|------|----------|------|
| PSGNet（全监督 SOTA 基线） | 28.84 | 17.77M 参数，CNN，无生成先验 |
| **AE 压缩上界（LDM 数学硬顶）** | 24.95 | LDM 系无论采样多完美也超不过 |
| 当前最优 LDM | **21.14±0.01** | 阶段3+FiLM + 测试时物理引导 s=0.8（DDPM100） |
| DDIM 少步（已证伪） | ~19 | 各步数均 ~19dB，低于 DDPM100 约 2dB（§11） |

**差距诊断（7.7dB vs PSGNet）：**
- AE 压缩损失 ≈ **3.9dB**（28.84−24.95）→ 只能靠改架构（L4）或图像空间工作绕开
- 扩散采样损失 ≈ **3.8dB**（24.95−21.14）→ 采样流程可压缩（L3 主攻方向）

================================================================================
一、物理注入方式的历史证据汇总（为什么需要换注入点）
================================================================================

历史上物理约束已尝试过**三种注入形式**，全部收效甚微或证伪：

### 1.1 训练 loss 注入（阶段4/5，= 下文方案 L2）——已做，三次证伪

| 实验 | 结果 | 结论 |
|------|------|------|
| 阶段4 配对场景 λ=0.1 | 21.52→21.78（+0.26dB），统一口径 3 种子 +0.28dB，p≈0.52 | 增益不显著；主要作用是稳定训练（std 0.76→0.25） |
| 阶段5 无配对场景 | λ_asm=0 消融：去掉 ASM 反而 **+0.44dB**（18.41 vs 17.97） | 物理自监督无正贡献，asm_loss 全程平坦 |
| 阶段8 强基线微调 | depth=2 **−1.73dB**（19.97→18.61）；FiLM **−2.06dB**（20.95→18.89） | **模型越强物理 loss 劣化越大** |

脚本: [train_physics_ldm_phase4.py](../scripts/train_physics_ldm_phase4.py)（L290-301 物理损失实现）
模块: [asm_module_phase4.py](../models/physics/asm_module_phase4.py)（注意 t/A 估计器输入含 clear，仅限训练期分析）

**机制结论（§9.2）**: 物理约束梯度与扩散去噪方向冲突——两个目标在同一组权重上拔河。

### 1.2 测试时梯度引导（§10，DCP/DPS 式）——已做，+0.18dB 且不迁移

| 实验 | 结果 | 结论 |
|------|------|------|
| DDPM100 + s=0.8（后半段 t≤50） | 20.97→21.15（**+0.18dB**） | 有效但量级噪声级 |
| DDIM 各步数 + s=0.8 | 持平或略降 | 引导增益依赖 DDPM 随机重噪的自纠错，DDIM 确定性路径下无增益（§11） |

**机制结论**: 梯度链路（latent→冻结有损 decoder→min 池化→标量）稀疏且不可信；
DCP 是 RGB 时代先验，与 182 波段波长相关散射错配；代理指标（暗通道均值）与真实目标
（PSNR/SAM）弱相关；手工弱先验与已学到的数据强先验（FiLM +2.61dB）拔河。

### 1.3 阶段5 物理自监督（DCP 初始化 + ASM 一致性 + EMA 自举）——已做

17.97±0.22dB，其中起作用的是 **DCP 初始化 + 扩散先验 + EMA 自举**，ASM 项被消融证伪（见 1.1）。

### 1.4 汇总表

| 注入点 | 状态 | 增益 | 判定 |
|--------|------|------|------|
| 训练 loss（L2） | 已做 | −2.06 ~ +0.28dB | **证伪**：梯度冲突 |
| 测试时引导 | 已做 | +0.18dB（仅 DDPM） | **弱**：不可迁移 |
| 物理作条件（L1） | **未做** | ? | 待验证 |
| 物理作前向过程（L3） | **未做** | ? | 待验证（推荐主线） |
| 物理作输出约束的精修（L4） | **未做** | ? | 待验证（对标 PSGNet） |

================================================================================
二、四级新方案（本次决策对象）
================================================================================

核心教训：物理不应作为**侧约束**与扩散目标拔河，而应成为扩散过程**本身的组成部分**。

--------------------------------------------------------------------------------
【P0】第 0 步：验证数据集造雾是否遵循 ASM（成本极低，先做）
--------------------------------------------------------------------------------
- [ ] 用现有 AtmosphericScatteringModel 拟合若干 (hazy, clear) 配对，
      对比物理重建残差 ‖I−(J·t+Â·(1−t))‖ 与 I/J 图像差异的量级
- [ ] 若残差 << 图像差异：造雾遵循 ASM → 物理参数按设计可恢复，
      L1/L3/L4 全部获得强辩护；若不遵循：转查数据集论文的雾合成公式
- 判据示例：物理残差/I−J 差异 < 10% 视为 ASM 可信

--------------------------------------------------------------------------------
【L1】物理量作条件输入（FiLM 路线的自然延伸）
--------------------------------------------------------------------------------
- 做法：DCP 闭式估计每张雾图的 t̂（dark_channel_prior，仅需雾图）、Â（暗通道最亮像素），
  拼接/下采样为 16ch latent 形状，作为第二条件通路注入 UNet（或与 z_hazy 拼接）
- 优势：网络通过端到端学习决定"物理估计可信多少"，而非盲目接受梯度方向；
  FiLM 已证明条件通路是最高杠杆点（+2.61dB）
- 改动量：小（ConditionalLatentUNet 增加条件分支 + 重训 phase3）
- 预期：+0.5~1dB；天花板仍为 AE 24.95dB
- 状态：未做（bandwise_condition 的 t 估计器输入含 clear，只是分析工具，不算条件注入）

--------------------------------------------------------------------------------
【L2】物理作训练损失——【已做过且证伪，不再重做】
--------------------------------------------------------------------------------
- 历史证据见 §1.1（阶段4/5/8 三次独立证伪：配对 +0.28 不显著、无配对 −0.44、
  强基线 −1.73/−2.06dB）
- 唯一值得保留的变体：在 L3 桥过程内部作为辅助项（此时物理已是主目标，冲突消除），
  但应作为 L3 的消融项而非独立路线
- 状态：**关闭**

--------------------------------------------------------------------------------
【L3】物理退化即前向过程（"清晰→雾"桥）——推荐主线
--------------------------------------------------------------------------------
- 做法：把扩散前向过程从"清晰→高斯噪声"改为物理退化桥：
      q(z_t | z_clear, z_hazy) = s_t·z_clear + (1−s_t)·z_hazy + σ_t·ε,  s_t: 1→0
  - 训练：t 均匀采样，UNet 在归一化 latent 空间学噪声/速度场；AE 与 FiLM UNet 结构复用
  - 采样：从 z_hazy 出发（不再是纯噪声），沿去雾轨迹走到 z_clear
- 为什么直击历史失败机制：
  1. 物理不再是侧约束而是主目标本身 → **梯度冲突按构造消除**
  2. 采样路径从"噪声→清晰"缩短为"雾→清晰" → **方向 B（少步采样）复活**，
     DDIM 失败的"长路径+无自纠错"根因被移除
  3. 每步 t 有物理含义（雾浓度插值），可解释性强
- 文献根基：I2SB / BBDM / Cold Diffusion / 配对流匹配（审稿可辩护）
- 改动量：中（train_conditional_ldm_phase3_full.py 的加噪公式 + 采样起点，
  ddim_step 的 x0 估计结构可复用）
- 预期：冲击 3.8dB 采样差距的大头；终点仍被 AE 24.95 盖住
- 状态：**代码完成（2026-08-31），端到端冒烟通过，待服务器训练**
  - models/diffusion/bridge_ldm.py（HazeBridge 调度 + sample_bridge，x0 预测无除法设计）
  - scripts/train_bridge_ldm.py（fork 自 phase3_full：桥前向 + x0 损失 + 雾起点采样）
  - scripts/eval_bridge.py（少步评测：steps×eta×seeds，含 steps=1 回归极限对照）
  - 冒烟验证：1 epoch 全链路跑通（缓存复用/训练/验证/可视化/全量 264 对测试）
  - 实测补充：AE 上限 per-image 口径复测为 25.38dB（train 25.22/test 25.38，泛化差距 -0.16dB，
    确认压缩有损而非泛化受限，见 results/metrics_guided/ae_ceiling_recheck.json）

--------------------------------------------------------------------------------
【L4】物理约束的图像空间精修（混合路线，对标 PSGNet）
--------------------------------------------------------------------------------
- 做法：L3 扩散粗修输出 + 雾图 + t̂ → 图像空间回归精修网络；
  输出约束在 ASM 反解附近（J_asm = (I−Â)/t̂ + Â 的残差）
- 优势：绕开 AE 24.95 天花板，上限对齐 PSGNet 28.84dB
- 定位：若目标是"整体超过 PSGNet"则必做；若目标是"扩散+物理叙事"则 L3 已足
- 状态：未做（依赖 L3 或 DDPM 粗修作为前置）

================================================================================
三、决策与执行顺序（待确认）
================================================================================

推荐路径：**P0 → L3 →（可选）L1 作为消融/辅助条件 → L4（若需对标 PSGNet）**

| 顺序 | 任务 | 产出物 | 判定标准 |
|------|------|--------|----------|
| 1 | P0 验证 ASM 假设 | results/asm_fit_check.json + 结论 | 残差占比 <10%？ |
| 2 | L3 桥训练脚本 | scripts/train_bridge_ldm.py | val PSNR 曲线 vs 21.14 基线 |
| 3 | L3 少步评测 | results/metrics_guided/*bridge*.json | 少步(10/25) ≥ DDPM100？ |
| 4 | L1 条件消融（可选） | 同口径对照 | +Δ 是否 ≥ 1×种子 std？ |
| 5 | L4 精修（可选） | scripts/train_refiner.py | 是否超过 PSGNet 28.84？ |

统一口径不变：best ckpt + blend=1.0 + 固定种子 + 全测试集 264 对 + 多种子 mean±std。
