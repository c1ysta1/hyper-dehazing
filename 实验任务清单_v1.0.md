# 高光谱去雾实验任务清单（物理引导扩散模型）
# 版本: v1.1
# 创建日期: 2026-08-11
# 修改日期: 2026-08-13
# 执行原则: 先做实验，再调方向；每步必须有可验证产出
# 数据说明: 使用本地雄安马蹄湾村航空高光谱数据（ENVI格式），通过大气散射模型合成配对雾霾图

================================================================================
阶段一: 环境搭建与数据准备 (第1-3天)
================================================================================

【任务1.1】创建项目目录结构
--------------------------------------------------------------------------------
命令:
  mkdir -p ~/hsi_dehaze/{data,models,scripts,results,checkpoints,logs}
  mkdir -p ~/hsi_dehaze/data/{xiong_an,hyperdehazing,raw,synthetic_haze}
  mkdir -p ~/hsi_dehaze/models/{psgnet,baseline,diffusion}

检查点:
  [ ] 执行ls ~/hsi_dehaze 能看到完整目录树
  [ ] 截图保存目录结构

--------------------------------------------------------------------------------

【任务1.2】安装Python环境
--------------------------------------------------------------------------------
命令:
  conda create -n hsi_dehaze python=3.10 -y
  conda activate hsi_dehaze
  pip install torch==2.1.0 torchvision==0.16.0 --index-url https://download.pytorch.org/whl/cu118
  pip install diffusers==0.24.0 transformers==4.35.0 accelerate==0.24.0
  pip install scipy numpy matplotlib scikit-image scikit-learn pillow tqdm tensorboard
  pip install h5py spectral torchmetrics einops
  pip install opencv-python-headless

检查点:
  [ ] python -c "import torch; print(torch.cuda.is_available())" 输出True
  [ ] python -c "import diffusers; print(diffusers.__version__)" 输出0.24.0
  [ ] nvidia-smi 能看到GPU信息

--------------------------------------------------------------------------------

【任务1.3】读取本地高光谱数据
--------------------------------------------------------------------------------
命令:
  # 数据已位于本地:
  # d:\Onedrive\OneDrive - neau.edu.cn\高光谱\实验代码\data\xiong_an_ma_ti_wan_cun_hang_kong_gao_etc\xiong_an_ma_ti_wan_cun_hang_kong_gao_etc\XiongAn.hdr
  # d:\Onedrive\OneDrive - neau.edu.cn\高光谱\实验代码\data\xiong_an_ma_ti_wan_cun_hang_kong_gao_etc\xiong_an_ma_ti_wan_cun_hang_kong_gao_etc\XiongAn.img
  cd ~/hsi_dehaze/data/xiong_an
  # 创建软链接/复制数据到项目目录（Windows下用copy或mklink）
  # copy "D:\...\XiongAn.*" . 或 mklink XiongAn.hdr "D:\...\XiongAn.hdr"

检查点:
  [ ] ls ~/hsi_dehaze/data/xiong_an 能看到 XiongAn.hdr 和 XiongAn.img
  [ ] 用spectral库读取成功: python -c "from spectral import envi; print(envi.open('XiongAn.hdr').shape)"
  [ ] 记录数据集尺寸: 高度、宽度、波段数

--------------------------------------------------------------------------------

【任务1.4】数据预处理与雾霾合成脚本
--------------------------------------------------------------------------------
创建文件: ~/hsi_dehaze/scripts/preprocess_xiong_an.py

功能要求:
  1. 读取本地ENVI数据（XiongAn.hdr + XiongAn.img）
  2. 剔除低质量波段（水汽吸收带、噪声带），保留有效波段
  3. 归一化到[0,1]
  4. 切分成patch（256x256或128x128，视原始尺寸调整）
  5. 基于大气散射模型合成配对雾霾图: I = J * t + A * (1 - t)
     - 透射率 t 与深度/距离相关，可用随机深度图或固定参数
     - 大气光 A 按波段随机或预设
  6. 划分train/test，保存为PyTorch的Dataset格式

检查点:
  [ ] 运行脚本不报错
  [ ] 输出目录有train/和test/子目录
  [ ] 打印统计信息: 训练样本数、测试样本数、波段数、空间尺寸
  [ ] 可视化3个样本: 清晰图、合成雾霾图、差异图，保存到~/hsi_dehaze/results/vis_xiong_an.png

================================================================================
阶段二: 基线复现 (第4-7天)
================================================================================

【任务2.1】复现PSGNet核心模块
--------------------------------------------------------------------------------
创建文件: ~/hsi_dehaze/models/psgnet/psgnet_core.py

必须实现的模块:
  1. SpectralGrouping (光谱分组)
  2. PSEB (Physics-aware Spectral Enhancement Block)
  3. FCB (Frequency-domain Construction Block) - 简化版，先不做FFT
  4. 基础U-Net编码器-解码器

检查点:
  [ ] 每个模块能独立运行单元测试
  [ ] 输入(1, 172, 256, 256) -> 输出(1, 172, 256, 256)，尺寸正确
  [ ] 统计参数量: sum(p.numel() for p in model.parameters())
  [ ] 记录: 参数量应与PSGNet论文接近（约50-60M）

--------------------------------------------------------------------------------

【任务2.2】训练PSGNet基线
--------------------------------------------------------------------------------
创建文件: ~/hsi_dehaze/scripts/train_psgnet.py

训练配置:
  - 数据集: 本地合成配对数据 train set
  - 批次大小: 1或2（根据显存调整）
  - 学习率: 0.0001
  - 优化器: Adam
  - 损失函数: L1 Loss + 可选的频域损失
  - 训练轮数: 100轮（先跑起来，不追求收敛）
  - 每10轮保存一次checkpoint

检查点:
  [ ] 训练能跑起来，不报错
  [ ] 显存占用稳定，不OOM
  [ ] 每轮打印loss，loss应呈下降趋势
  [ ] 保存的checkpoint文件大小正常（约200MB）
  [ ] TensorBoard日志正常: tensorboard --logdir=~/hsi_dehaze/logs/psgnet

--------------------------------------------------------------------------------

【任务2.3】PSGNet测试与指标计算
--------------------------------------------------------------------------------
创建文件: ~/hsi_dehaze/scripts/test_psgnet.py

必须计算的指标:
  - PSNR (Peak Signal-to-Noise Ratio)
  - SSIM (Structural Similarity)
  - SAM (Spectral Angle Mapper) - 高光谱特有指标
  - 推理时间（单张图片）

检查点:
  [ ] 在本地合成 test set 上运行
  [ ] 输出平均PSNR、SSIM、SAM
  [ ] 与PSGNet论文指标对比（论文: PSNR=31.76, SSIM=0.9612）作为参考
  [ ] 可视化5个测试样本的对比图（输入/输出/GT），保存到results/
  [ ] 记录: 即使指标不如论文，也要记录差距，分析原因

================================================================================
阶段三: 扩散模型基础实验 (第8-12天)
================================================================================

【任务3.1】搭建简化版DDPM
--------------------------------------------------------------------------------
创建文件: ~/hsi_dehaze/models/diffusion/simple_ddpm.py

要求:
  1. 实现基础的DDPM前向加噪过程
  2. 实现基础的U-Net去噪网络（2D，先不做3D）
  3. 在MNIST或CIFAR-10上验证（确保扩散模型本身正确）
  4. 然后适配到高光谱（输入通道从3改为172）

检查点:
  [ ] MNIST实验: 从纯噪声生成清晰数字，可视化结果正常
  [ ] 高光谱适配: 输入(1, 172, 64, 64) -> 输出(1, 172, 64, 64)
  [ ] 训练1个epoch能跑通，loss下降

--------------------------------------------------------------------------------

【任务3.2】实现Latent Diffusion基础框架
--------------------------------------------------------------------------------
创建文件: ~/hsi_dehaze/models/diffusion/ldm_hsi.py

要求:
  1. 编码器: 高光谱(172ch) -> 潜在空间(如16ch)
  2. 在潜在空间做DDPM
  3. 解码器: 潜在空间 -> 高光谱(172ch)
  4. 先不用条件，做无条件生成测试

检查点:
  [ ] 自编码器能重建清晰图: 输入清晰图 -> 编码 -> 解码 -> 输出应接近输入
  [ ] 重建PSNR > 30dB（证明潜在空间信息保留充分）
  [ ] 潜在空间维度: 打印编码后特征图尺寸，确认压缩比

--------------------------------------------------------------------------------

【任务3.3】条件扩散：加入雾霾图作为条件
--------------------------------------------------------------------------------
创建文件: ~/hsi_dehaze/models/diffusion/conditional_ldm.py

要求:
  1. 将雾霾图通过编码器得到条件特征
  2. 在扩散过程中，将条件特征注入U-Net的每一层
  3. 训练目标: 输入(雾霾图) + 噪声 -> 预测清晰图的潜在表示

检查点:
  [ ] 训练能跑通
  [ ] 测试: 输入雾霾图 -> 生成去雾结果
  [ ] 计算PSNR/SSIM/SAM，记录基线分数
  [ ] 与PSGNet对比: 扩散模型 vs CNN，哪个更好？

================================================================================
阶段四: 物理约束模块 (第13-17天)
================================================================================

【任务4.1】实现大气散射模型(ASM)模块
--------------------------------------------------------------------------------
创建文件: ~/hsi_dehaze/models/physics/asm_module.py

要求:
  1. 输入: 雾霾图 I，清晰图 J（训练时用GT，推理时未知）
  2. 估计透射率 t(x): 基于暗通道先验或网络预测
  3. 估计大气光 A: 全局或局部
  4. 物理一致性损失: L_phys = ||I - (J * t + A * (1-t))||_1
  5. 波段自适应: 每个波段有自己的t_k和A_k

检查点:
  [ ] 单独测试ASM: 给定I,J，能计算出合理的t和A
  [ ] 可视化t(x)的灰度图，应显示雾霾厚度分布
  [ ] 波段自适应: 打印不同波段的t值，短波长t应更小（雾霾更严重）

--------------------------------------------------------------------------------

【任务4.2】物理引导扩散：将ASM嵌入训练
--------------------------------------------------------------------------------
创建文件: ~/hsi_dehaze/scripts/train_physics_ldm.py

训练配置:
  - 总损失 = 扩散损失 + λ * 物理一致性损失
  - λ = 0.1（先设小，后续调参）
  - 其他配置同任务3.3

检查点:
  [ ] 训练能跑通，两个损失都下降
  [ ] 对比实验: 有物理约束 vs 无物理约束
  [ ] 指标对比: SAM应显著改善（光谱保真度提升）
  [ ] 可视化光谱曲线: 同一像素，有/无物理约束的光谱曲线对比

--------------------------------------------------------------------------------

【任务4.3】波段自适应物理条件
--------------------------------------------------------------------------------
创建文件: ~/hsi_dehaze/models/physics/bandwise_condition.py

要求:
  1. 设计波段分组策略（参考PSGNet: 5组）
  2. 每组共享一个透射率估计网络
  3. 短波长组（可见光）：更关注雾霾去除
  4. 长波长组（红外）：更关注细节保留
  5. 组间信息传递: 从长波组向短波组传递清晰特征

检查点:
  [ ] 波段分组可视化: 每组包含哪些波段
  [ ] 每组透射率估计值打印，验证物理直觉
  [ ] 消融实验: 统一透射率 vs 波段自适应透射率

================================================================================
阶段五: 无配对训练实验 (第18-22天)
================================================================================

【任务5.1】设计无配对训练策略
--------------------------------------------------------------------------------
创建文件: ~/hsi_dehaze/scripts/train_unpaired.py

核心思想:
  - 训练数据只有雾霾图，没有清晰图
  - 用ASM作为自监督信号
  - 扩散模型学习: 从噪声 -> 满足物理约束的清晰图

实现方案:
  1. 用预训练的清晰图像生成器（或无条件扩散模型）作为"伪清晰图"来源
  2. 或者用GAN的循环一致性思想
  3. 损失: L = L_diffusion + λ1 * L_asm + λ2 * L_cycle

检查点:
  [ ] 只用雾霾图训练，模型能收敛
  [ ] 测试时输入雾霾图，输出去雾结果
  [ ] 与有配对训练对比: 性能差距多大？
  [ ] 这是核心创新点，必须记录详细实验数据

================================================================================
阶段六: 结果整理与汇报准备 (第23-25天)
================================================================================

【任务6.1】整理所有实验结果
--------------------------------------------------------------------------------
创建文件: ~/hsi_dehaze/results/experiment_summary.md

必须包含的表格:
  1. 方法对比表: PSGNet / 简单LDM / 物理引导LDM / 无配对LDM
  2. 指标: PSNR, SSIM, SAM, 参数量, 推理时间
  3. 消融实验表: 有/无物理约束, 统一/波段自适应透射率

检查点:
  [ ] 所有实验数据汇总在一个文件
  [ ] 关键结果可视化图表（至少5张）

--------------------------------------------------------------------------------

【任务6.2】准备向姜教授汇报
--------------------------------------------------------------------------------
汇报PPT/文档结构:
  1. 实验目标回顾
  2. 数据集统计
  3. PSGNet复现结果（基线）
  4. 扩散模型基线结果
  5. 物理约束模块效果（消融实验）
  6. 无配对训练初步结果（如果有）
  7. 下一步计划 + 需要姜教授指导的问题

检查点:
  [ ] 汇报材料能在15分钟内讲完
  [ ] 有具体数字，不笼统
  [ ] 明确列出3个需要确认的问题

================================================================================
关键检查点汇总（每周必做）
================================================================================

第1周末:
  [ ] 环境搭建完成，GPU可用
  [ ] 本地ENVI数据读取并预处理完成
  [ ] 可视化样本保存

第2周末:
  [ ] PSGNet核心模块实现
  [ ] 训练跑通（即使不收敛）

第3周末:
  [ ] 简化DDPM在MNIST上验证通过
  [ ] LDM高光谱适配完成

第4周末:
  [ ] ASM模块实现并测试
  [ ] 物理引导扩散训练跑通

第5周末:
  [ ] 无配对训练实验（至少尝试）
  [ ] 所有对比实验数据收集完毕

================================================================================
风险预案
================================================================================

风险1: 本地ENVI数据读取失败
  应对: 检查spectral库安装；确认hdr与img文件路径一致；如仍失败，先用HyperDehazing数据集或自己生成模拟数据

风险2: 显存不足（OOM）
  应对: 
    - 减小batch_size到1
    - 减小patch_size到128x128
    - 使用混合精度: torch.cuda.amp
    - 减小潜在空间维度

风险3: PSGNet复现指标远低于论文
  应对: 
    - 正常现象，先记录差距
    - 检查预处理是否正确（波段剔除、归一化）
    - 训练轮数可能不够，先跑100轮看趋势

风险4: 扩散模型训练不稳定
  应对:
    - 降低学习率（如1e-5）
    - 使用预训练的自编码器
    - 先在小patch(64x64)上验证

================================================================================
文件命名规范
================================================================================

代码文件:
  scripts/train_<方法>_<日期>.py
  models/<模块>/<模块名>.py

实验结果:
  results/exp<编号>_<方法>_<日期>/
  包含: metrics.json, sample_vis.png, loss_curve.png

检查点:
  checkpoints/<方法>_epoch<轮数>_<日期>.pth

日志:
  logs/<方法>_<日期>.log
