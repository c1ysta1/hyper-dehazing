# 高光谱去雾实验任务清单（实证版）
# 版本: v2.0
# 创建日期: 2026-08-28
# 上游: 实验任务清单_v1.0.md（初始 6 阶段规划，已被实际执行超越/证伪）
# 证据目录: results/experiment_summary.md §1-§10、logs/、checkpoints/
# 统一评测口径: best ckpt + blend=1.0 + 固定采样种子 + 全测试集 264 对 + 多种子 mean±std

================================================================================
〇、当前坐标系（先看这里）
================================================================================

| 目标 | PSNR(dB) | 说明 |
|------|----------|------|
| PSGNet（全监督 SOTA 基线） | 28.84 | 17.77M 参数，CNN，无生成先验 |
| **AE 压缩上界（LDM 数学硬顶）** | 24.95 | LDM 系无论采样多完美也超不过 |
| 当前最优 LDM | **21.14±0.01** | 阶段3+FiLM + 测试时物理引导 s=0.8 |
| 无条件/无配对 | 17.97±0.22 | 仅雾图训练（无ASM更高 18.41） |

**整体差距诊断（7.7dB vs PSGNet）：**
- AE 压缩损失 ≈ **3.9dB**（28.84−24.95）→ 属架构天花板，改 AE 才治本
- 扩散采样损失 ≈ **3.8dB**（24.95−21.14）→ 采样/UNet 优化可压缩
- 物理引导只补 **0.18dB** → 它救不了整体差距，是"点睛"不是"地基"

-------------------------------------------------------------------------------
结论：当前 LDM 架构内追不平 PSGNet；要"去雾整体成功"必须抬 AE 上界 + 压缩采样损失。
-------------------------------------------------------------------------------

================================================================================
一、已完成实验流程（按研发顺序）
================================================================================

【阶段1 环境与数据】
- [x] 环境搭建、GPU 可用、本地数据处理（合成/分包 train/clear/test）
- [x] 数据集 RSyntHyperPDID：182 波段 256×256；训练 2352 对 / 测试 264 对；
      配对规则：雾图 XXXX_YYYY.npy ↔ clear/XXXX.npy；clear=GT

【阶段2 PSGNet 基线】
- [x] 结构复现 + 训练 + 评估：PSNR 28.84 / SSIM 0.8911 / SAM 6.13°，17.77M 参数

【阶段3 条件 LDM（AE + 条件UNet + 扩散）】
- [x] AE：182→16×64×64，重建 24.95dB（上界来源）
- [x] 条件LDM：原始结构多种子 19.99±0.76dB

【阶段4 物理引导 LDM（训练 loss 注入）】
- [x] ASM 模块 + 透射率波段自适应（重建 L1 较统一低 3.5%）
- [x] 训练 loss 注入物理约束 20.27±0.25dB，但 3 种子配对增益 p≈0.52 不显著
- [x] 【证伪】模型越强物理 loss 劣化越明显（下表）

【阶段5 无配对训练】
- [x] DCP 初始化 + 扩散先验 + EMA 自举：17.97±0.22dB（优于纯 DCP 12.89dB 达 +5.08）
- [x] 【证伪】λ_asm=0 消融证实去掉 ASM 反而 +0.44dB（物理自监督无正贡献）

【阶段6 统一口径修订 + 汇报】
- [x] 修正 blend=0.9/last-ckpt/小子集选优 三处口径缺陷
- [x] AE 上界 24.95dB 的差距分解（§0）

【阶段8 容量扩展实验（2026-08-27）】
- [x] AE 通道 16→32/64ch：【证伪】重建反而变差（24.63/24.71 < 24.95）
- [x] UNet depth 1→2：阶段3 +0.49dB，但阶段4 物理微调 **−1.73dB**（大模型上物理 loss 爆炸/坍塌）；修复 lr=2e-4+梯度裁剪

【阶段9 UNet 内部模块增强（2026-08-27）——当前最优训练路线】
- [x] SpatialFiLM (+0.17M) 把阶段3 从 18.34 → **20.95dB（+2.61）**，远超加深(+0.49/10M)
- [x] SE +2.54 / CBAM +2.16 → 条件注入通路是真正瓶颈
- [x] 强基线 FiLM 上 phase4 微调 → −2.06dB（再次印证物理 loss 结构化冲突）

【阶段10 测试时物理引导（2026-08-28）——物理叙事唯一正解】
- [x] DCP 梯度采样引导，训练零改动；梯度归一化 + 后半段(t≤50) + s=0.8
- [x] 多种子 {42,100,200} 验证：**20.96±0.01 → 21.14±0.01（+0.18±0.00 完全一致）**

================================================================================
二、被证伪的路线（写入论文的价值 = 负结果也是贡献）
================================================================================

| 路线 | 结论 | 机制 |
|------|------|------|
| 物理约束当训练 loss 注入 | 强基线必劣化（FiLM −2.06 / d2 −1.73） | 物理梯度穿透采样链，与扩散噪声预测目标冲突 |
| 阶段5 ASM 物理自监督 | 无正贡献（去 ASM 反 +0.44） | asm_loss 平坦不动，梯度方向与去噪冲突 |
| AE 潜在通道加宽 | 重建退化（32/64ch < 16ch） | 压缩瓶颈不在通道数 |
| UNet 加深 | 阶段3 微升但全链路无收益 | 10M 参数收益 < 0.5M 模块，条件注入才是瓶颈 |

================================================================================
三、待决策项（需与导师对齐）
================================================================================

1. 扩散路线定位：是"无配对/生成先验泛化"方法论，还是必须追平 PSGNet 指标？
2. 阶段5 无配对是否保留"物理自监督"叙事（证据支持移除 ASM）？
3. 测试时物理引导是否作为论文核心卖点（首次不损害训练的稳定正增益 +0.18dB）？

================================================================================
四、后续方向优先级（针对"去雾整体未成功"）
================================================================================

推荐 **A+B 组合攻坚**，是唯一能真正逼近 PSGNet 的路径：

- [ ] **A. 抬 AE 上界（治本）**：降低空间压缩比（64×64→128×128 latent 或 4×→2×），
      重训 AE，期望上界 24.95→27+，再级联重训阶段3→评测。（关键前提：证伪的是"加宽通道"，
      不是"降低分辨率压缩"，后者从未试过）
- [ ] **B. 压缩采样损失（就地榨干）**：DDIM/DPM-Solver 少步采样、或"扩散粗修+回归精修"混合；
      把 21.14 往新上界推，且可与物理引导叠加保留
- [ ] **C. 强化引导先验（点睛）**：辐射传输方程估 t(x)/A 或 ASM 引导替代单一 DCP，
      提升引导上限（预期仍为小数点级，不作为主攻）
- [ ] **D. 叙事重构（兜底，不动架构）**：接受非 SOTA 指标，卖方法论贡献
      （FiLM 消融 +2.61、物理训练 loss 三次证伪+机制、测试时引导 +0.18 首次成立）

若时间紧/算力有限 → 选 **D+B**（不重训 AE，只做采样端优化 + 叙事重构）。

================================================================================
附：核心复现命令
================================================================================
# 模块升级（FiLM 最优）
env -u LD_PRELOAD setsid nohup bash scripts/run_module_upgrade.sh > logs/module_upgrade.log 2>&1 &
# 测试时物理引导评测（多种子）
python scripts/eval_physics_guidance.py --guidance_s 0 0.8 --seeds 42 100 200
# 汇总
results/experiment_summary.md
================================================================================